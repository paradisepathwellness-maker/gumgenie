# Frontend
# Vite will read .env.local automatically; keep secrets out of the client bundle.
# If you want the frontend to call a different backend host:
# VITE_API_BASE_URL=http://localhost:4000

# AI (used by backend/server-side generation endpoints)
GEMINI_API_KEY=

# Backend
PORT=4000
FRONTEND_URL=http://localhost:3110

# Gumroad OAuth (required for real publishing)
# Create an OAuth app in Gumroad and set:
# Redirect URI (recommended for local dev):
#   http://localhost:4000/api/gumroad/oauth/callback
GUMROAD_CLIENT_ID=
GUMROAD_CLIENT_SECRET=
GUMROAD_REDIRECT_URI=http://localhost:4000/api/gumroad/oauth/callback
GUMROAD_ACCESS_TOKEN=

# MCP (stdio) local servers
# Used to spawn @21st-dev/magic MCP server. Do NOT commit real keys.
MAGIC_API_KEY=

# MCP (SSE hosted) servers
# Store full Unframer SSE URL here. DO NOT commit real secrets.
MCP_UNFRAMER_SSE_URL=

# Gemini Image MCP (local HTTP service)
# Used by backend MCP adapter to talk to the running gemini-image-mcp container.
# This is NOT a secret.
GEMINI_IMAGE_MCP_BASE_URL=http://localhost:3102

# Optional: override backend base URL used to construct local MCP adapter URLs.
# This is NOT a secret.
MCP_ADAPTER_BASE_URL=http://localhost:4000

# Apify 
# Used to run actors, scrape data, extract information, identify pain points, and generate key insights.
APIFY_TOKEN=

# SERP API 
# Used to scrape data, extract information, identify pain points, and generate key insights.
SERP_API_KEY=

# Stock image APIs (server-side only)
# DO NOT expose these in Vite client code.
# Official docs:
# - Pexels: https://www.pexels.com/api/documentation/
# - Pixabay: https://pixabay.com/api/docs/
PEXELS_API_KEY=
PIXABAY_API_KEY=

# Notion OAuth credentials
NOTION_CLIENT_ID=
NOTION_CLIENT_SECRET=
NOTION_REDIRECT_URI=

# RovoDev
ROVODEV_BASE_URL=http://localhost:5555
ROVODEV_API_TOKEN=
